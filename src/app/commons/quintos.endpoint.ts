import { environment } from 'src/environments/environment';

export class QuintosEndpoint {

    public static API_REGISTRAR_FOTO = environment.API_URL + '/foto';
    public static API_REGISTRAR_MENSAJE = environment.API_URL + '/mensaje';
    
}
